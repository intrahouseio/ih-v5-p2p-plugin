'use strict';
const os = require('os');

const file = `${os.type()}_${os.arch()}`.toLocaleLowerCase();


try {
  module.exports = require('../build/Release/' + file + '.node');
} catch (error) {
  module.exports = require('../build/Release/wrtc.node');
}